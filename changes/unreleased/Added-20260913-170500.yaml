kind: Added
body: 'Two published figures are now gated against the binary that were not: the
  routing and depth targets on the coverage page, and the size of the shipped
  binary (45 MiB ceiling, checked in CI). A third gate asserts that all 431
  registered services initialize, which main.go only warned about, but its startup
  time is logged rather than gated'
custom:
    Issue: "163"
