// Code generated by devcloud codegen. DO NOT EDIT.
// SPDX-License-Identifier: Apache-2.0
package managedblockchain

import "strings"

// OperationRoute maps an HTTP method and URI pattern to an operation name.
type OperationRoute struct {
	Method    string
	Pattern   string
	Operation string
}

// OperationRoutes is the list of all routes for this service.
var OperationRoutes = []OperationRoute{
	{Method: "POST", Pattern: "/accessors", Operation: "CreateAccessor"},
	{Method: "POST", Pattern: "/networks/{NetworkId}/members", Operation: "CreateMember"},
	{Method: "POST", Pattern: "/networks", Operation: "CreateNetwork"},
	{Method: "POST", Pattern: "/networks/{NetworkId}/nodes", Operation: "CreateNode"},
	{Method: "POST", Pattern: "/networks/{NetworkId}/proposals", Operation: "CreateProposal"},
	{Method: "DELETE", Pattern: "/accessors/{AccessorId}", Operation: "DeleteAccessor"},
	{Method: "DELETE", Pattern: "/networks/{NetworkId}/members/{MemberId}", Operation: "DeleteMember"},
	{Method: "DELETE", Pattern: "/networks/{NetworkId}/nodes/{NodeId}", Operation: "DeleteNode"},
	{Method: "GET", Pattern: "/accessors/{AccessorId}", Operation: "GetAccessor"},
	{Method: "GET", Pattern: "/networks/{NetworkId}/members/{MemberId}", Operation: "GetMember"},
	{Method: "GET", Pattern: "/networks/{NetworkId}", Operation: "GetNetwork"},
	{Method: "GET", Pattern: "/networks/{NetworkId}/nodes/{NodeId}", Operation: "GetNode"},
	{Method: "GET", Pattern: "/networks/{NetworkId}/proposals/{ProposalId}", Operation: "GetProposal"},
	{Method: "GET", Pattern: "/accessors", Operation: "ListAccessors"},
	{Method: "GET", Pattern: "/invitations", Operation: "ListInvitations"},
	{Method: "GET", Pattern: "/networks/{NetworkId}/members", Operation: "ListMembers"},
	{Method: "GET", Pattern: "/networks", Operation: "ListNetworks"},
	{Method: "GET", Pattern: "/networks/{NetworkId}/nodes", Operation: "ListNodes"},
	{Method: "GET", Pattern: "/networks/{NetworkId}/proposals/{ProposalId}/votes", Operation: "ListProposalVotes"},
	{Method: "GET", Pattern: "/networks/{NetworkId}/proposals", Operation: "ListProposals"},
	{Method: "GET", Pattern: "/tags/{ResourceArn}", Operation: "ListTagsForResource"},
	{Method: "DELETE", Pattern: "/invitations/{InvitationId}", Operation: "RejectInvitation"},
	{Method: "POST", Pattern: "/tags/{ResourceArn}", Operation: "TagResource"},
	{Method: "DELETE", Pattern: "/tags/{ResourceArn}", Operation: "UntagResource"},
	{Method: "PATCH", Pattern: "/networks/{NetworkId}/members/{MemberId}", Operation: "UpdateMember"},
	{Method: "PATCH", Pattern: "/networks/{NetworkId}/nodes/{NodeId}", Operation: "UpdateNode"},
	{Method: "POST", Pattern: "/networks/{NetworkId}/proposals/{ProposalId}/votes", Operation: "VoteOnProposal"},
}

// MatchOperation returns the operation name and extracted path parameters for
// the given HTTP method and URI path. It returns ("", nil) if no route matches.
func MatchOperation(method, path string) (string, PathParams) {
	for _, route := range OperationRoutes {
		if route.Method != method {
			continue
		}
		if params, ok := matchURI(route.Pattern, path); ok {
			return route.Operation, params
		}
	}
	return "", nil
}

// matchURI matches a URI path against a pattern that may contain {Label} and
// {Label+} (greedy) segments. Returns the extracted parameters and true on
// match, or nil and false otherwise.
func matchURI(pattern, path string) (PathParams, bool) {
	patParts := splitPath(pattern)
	pathParts := splitPath(path)

	params := PathParams{}
	pi := 0
	for i, pat := range patParts {
		if strings.HasPrefix(pat, "{") && strings.HasSuffix(pat, "+}") {
			// Greedy label: consumes all remaining path segments.
			label := pat[1 : len(pat)-2]
			if pi >= len(pathParts) {
				return nil, false
			}
			params[label] = joinPath(pathParts[pi:])
			pi = len(pathParts)
			// Greedy must be the last pattern segment.
			if i != len(patParts)-1 {
				return nil, false
			}
		} else if strings.HasPrefix(pat, "{") && strings.HasSuffix(pat, "}") {
			// Single-segment label.
			label := pat[1 : len(pat)-1]
			if pi >= len(pathParts) {
				return nil, false
			}
			params[label] = pathParts[pi]
			pi++
		} else {
			// Literal segment.
			if pi >= len(pathParts) || pathParts[pi] != pat {
				return nil, false
			}
			pi++
		}
	}
	if pi != len(pathParts) {
		return nil, false
	}
	return params, true
}

// splitPath splits a URI path into non-empty segments.
func splitPath(path string) []string {
	parts := strings.Split(strings.Trim(path, "/"), "/")
	out := parts[:0]
	for _, p := range parts {
		if p != "" {
			out = append(out, p)
		}
	}
	return out
}

// joinPath joins path segments back with "/" separators.
func joinPath(parts []string) string {
	return strings.Join(parts, "/")
}
